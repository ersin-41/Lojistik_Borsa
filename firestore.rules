rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Geliştirme aşaması için genel izin (Daha sonra kısıtlanmalı)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
